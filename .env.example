# ===== 本地开发环境变量示例 =====
# 复制为 .env 后按需填写。.env 已在 .gitignore 中，不会被提交。

# 后端
DEBUG=true
DATABASE_URL=sqlite:///./data/power_map.db
CORS_ORIGINS=http://localhost:8220

# LLM（数据 Pipeline 使用，Phase 5 启用）
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o
